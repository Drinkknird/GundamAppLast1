import 'package:flutter/material.dart';

class DetailScreen extends StatelessWidget {
  final String name;
  final String message;

  DetailScreen(this.name, this.message);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}